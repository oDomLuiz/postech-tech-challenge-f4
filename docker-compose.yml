services:
  lstm-stock-api:
    build: .
    container_name: lstm-stock-api
    ports:
      - "8000:8000"
    volumes:
      - ./models:/app/models
      - ./data:/app/data
    environment:
      - PYTHONUNBUFFERED=1
